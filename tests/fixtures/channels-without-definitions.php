<?php

// Fixture standing in for the routes/channels.php Laravel generates and the
// application never fills in. Every private subscribe is refused against this
// file, and nothing anywhere says why.

use Illuminate\Support\Facades\Broadcast;
